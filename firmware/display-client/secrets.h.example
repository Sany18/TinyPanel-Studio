// Copy this file to secrets.h (same folder) and fill in your WiFi credentials.
// secrets.h is gitignored and must never be committed.

#define WIFI_SSID     "your-wifi-name"
#define WIFI_PASSWORD "your-wifi-password"
